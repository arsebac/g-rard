{
  "name": "gerard",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "server",
    "client",
    "mcp"
  ],
  "scripts": {
    "dev": "npm run build -w mcp && concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client && npm run build -w mcp && npm run build -w server",
    "db:migrate": "npm run db:migrate -w server",
    "db:seed": "npm run db:seed -w server",
    "db:studio": "npm run db:studio -w server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
